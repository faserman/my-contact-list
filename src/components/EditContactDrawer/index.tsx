import React from 'react';
import './index.css';
import { observer } from 'mobx-react';

export const EditContactDrawer = observer((props: any) => {

  return(
    <div className="contact-list">
      
    </div>
  )
})